# 单围棋联网对战功能部署指南

## 一、准备工作

### 1. 检查云开发环境
确保 `miniprogram/envList.js` 文件中已配置正确的云开发环境ID：
```javascript
const envId = 'cloud1-d2gvjggqq39810145'; // 你的环境ID
```

### 2. 安装云函数依赖
进入每个云函数目录安装依赖：

```bash
# 安装匹配系统依赖
cd cloudfunctions/matchSystem
npm install

# 安装匹配检查器依赖
cd ../matchChecker
npm install

# 安装游戏同步依赖
cd ../gameSync
npm install
```

## 二、数据库集合创建

在微信开发者工具的云开发控制台中，创建以下集合：

### 1. `players` - 玩家信息表
字段：
- `_id` (自动生成)
- `openid` (string)
- `nickname` (string)
- `avatar` (string)
- `elo_rating` (number, 默认1200)
- `total_games` (number, 默认0)
- `wins` (number, 默认0)
- `losses` (number, 默认0)
- `draws` (number, 默认0)
- `created_at` (timestamp)
- `updated_at` (timestamp)

**索引**：
- `openid` (唯一索引)

### 2. `match_queue` - 匹配队列表
字段：
- `_id` (自动生成)
- `player_id` (string)
- `openid` (string)
- `nickname` (string)
- `avatar` (string)
- `elo_rating` (number)
- `join_time` (timestamp)
- `status` (string: waiting/matched/cancelled/expired)
- `matched_game_id` (string, 可空)
- `matched_player_id` (string, 可空)
- `created_at` (timestamp)

**索引**：
- `openid` (唯一索引，确保一个玩家只能在一个队列中)
- `status` + `join_time` (用于查找等待时间最长的玩家)

### 3. `games` - 对局信息表
字段：
- `_id` (自动生成)
- `black_player_id` (string)
- `white_player_id` (string)
- `black_openid` (string)
- `white_openid` (string)
- `black_nickname` (string)
- `white_nickname` (string)
- `black_avatar` (string)
- `white_avatar` (string)
- `board_size` (number: 13/15/19)
- `current_player` (string: black/white)
- `board_state` (array)
- `move_count` (number, 默认0)
- `status` (string: waiting/playing/black_win/white_win/draw/black_resign/white_resign/disconnected/cancelled)
- `winner` (string: black/white/draw, 可空)
- `winner_reason` (string, 可空)
- `start_time` (timestamp)
- `end_time` (timestamp, 可空)
- `last_move_time` (timestamp)
- `created_at` (timestamp)
- `updated_at` (timestamp)

**索引**：
- `black_openid` + `status`
- `white_openid` + `status`
- `status` + `updated_at`

### 4. `moves` - 棋步记录表
字段：
- `_id` (自动生成)
- `game_id` (string)
- `move_number` (number)
- `player` (string: black/white)
- `row` (number)
- `col` (number)
- `piece` (number: 1=黑, 2=白)
- `timestamp` (timestamp)
- `board_state_before` (array)
- `board_state_after` (array)
- `game_result` (string: playing/black_win/white_win/draw/null)
- `is_undo` (boolean, 默认false)
- `created_at` (timestamp)

**索引**：
- `game_id` + `move_number`
- `game_id` + `player`

### 5. `undo_requests` - 悔棋请求表
字段：
- `_id` (自动生成)
- `game_id` (string)
- `requester` (string: black/white)
- `target_move_number` (number)
- `reason` (string, 可空)
- `status` (string: pending/approved/rejected/expired)
- `response_by` (string: black/white, 可空)
- `response_time` (timestamp, 可空)
- `created_at` (timestamp)
- `updated_at` (timestamp)

**索引**：
- `game_id` + `status`

## 三、云函数部署

### 方法一：使用微信开发者工具
1. 打开微信开发者工具
2. 选择当前项目
3. 点击"云开发"按钮
4. 在"云函数"标签页中：
   - 右键点击"新建Node.js云函数"
   - 输入函数名称
   - 将对应目录的代码复制进去
   - 点击"上传并部署"

### 方法二：使用CLI命令
```bash
# 部署所有云函数
tcb functions:deploy matchSystem -e your-env-id
tcb functions:deploy matchChecker -e your-env-id
tcb functions:deploy gameSync -e your-env-id
```

## 四、配置云函数触发器

### 1. 匹配检查器定时触发
`matchChecker` 云函数需要定时执行以检查匹配：

在云开发控制台设置：
1. 进入"云函数" -> `matchChecker`
2. 点击"触发管理"
3. 添加定时触发器：
   - 触发周期：每5秒一次
   - Cron表达式：`*/5 * * * * * *`
   - 备注：匹配检查

### 2. 数据库权限设置
所有集合的权限设置为：
- 创建：仅创建者可写
- 读取：所有用户可读
- 更新：仅创建者可写
- 删除：仅创建者可写

## 五、前端配置检查

### 1. 检查 `app.json` 路由
确保包含以下页面：
```json
"pages": [
  "pages/index/index",
  "pages/room/room",
  "pages/online/online",
  "pages/match/index",
  "pages/game-online/index",
  "pages/game/game",
  "pages/ai-setup/ai-setup",
  "pages/game-ai/game-ai",
  "pages/rules/rules",
  "pages/settings/settings"
]
```

### 2. 检查 `app.js` 初始化
确保已正确初始化云开发和用户信息：
```javascript
onLaunch: function () {
  if (wx.cloud && envId) {
    wx.cloud.init({ env: envId, traceUser: true });
    this.cloudInited = true;
    this.initUserInfo();
  }
}
```

## 六、功能测试

### 1. 匹配功能测试
1. 进入小程序首页
2. 点击"联网对战"
3. 验证：
   - 是否显示匹配等待界面
   - 倒计时是否正确
   - 取消匹配是否正常

### 2. 游戏功能测试
1. 匹配成功后进入游戏
2. 验证：
   - 棋盘显示是否正确
   - 落子是否正常
   - 悔棋功能是否正常
   - 认输功能是否正常
   - 游戏结束判断是否正确

### 3. 网络异常测试
1. 断开网络连接
2. 验证：
   - 是否显示网络错误
   - 重连后是否能恢复

## 七、常见问题解决

### 1. 云函数部署失败
- **问题**：依赖安装失败
- **解决**：检查 `package.json` 文件，确保 `wx-server-sdk` 依赖正确

### 2. 数据库权限错误
- **问题**：无法读取/写入数据
- **解决**：检查数据库集合权限设置

### 3. 匹配功能不工作
- **问题**：匹配检查器未触发
- **解决**：检查定时触发器配置

### 4. 游戏同步延迟
- **问题**：棋盘状态不同步
- **解决**：检查轮询间隔，或考虑使用实时数据库watch

## 八、性能优化建议

### 1. 实时同步优化
- 使用云数据库的watch功能替代轮询
- 减少不必要的数据传输

### 2. 匹配算法优化
- 根据在线玩家数量动态调整匹配策略
- 添加匹配优先级的玩家偏好设置

### 3. 数据库优化
- 添加合适的索引
- 定期清理过期数据
- 使用分页查询大量数据

## 九、扩展功能建议

### 1. 观战功能
- 允许其他玩家观战
- 添加观战聊天功能

### 2. 排行榜系统
- 基于ELO等级分的排行榜
- 赛季系统

### 3. 社交功能
- 好友系统
- 邀请对战
- 聊天功能

## 十、监控和维护

### 1. 监控指标
- 在线玩家数量
- 匹配成功率
- 游戏平均时长
- 错误率

### 2. 定期维护
- 清理过期匹配记录
- 备份重要数据
- 更新ELO等级分算法

---

**部署完成检查清单**：
- [ ] 数据库集合创建完成
- [ ] 云函数部署完成
- [ ] 定时触发器配置完成
- [ ] 前端页面配置正确
- [ ] 功能测试通过
- [ ] 权限设置正确

如有问题，请参考微信开发者文档或联系技术支持。